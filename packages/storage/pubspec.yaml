name: proximity_storage
description: SQLite tally (prof) + secure storage (keys/templates).
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.4.0

dependencies:
  proximity_protocol:
    path: ../protocol
  meta: ^1.12.0

dev_dependencies:
  test: ^1.25.0
  lints: ^5.0.0

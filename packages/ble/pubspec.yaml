name: proximity_ble
description: BLE advertise/scan/GATT/relay engine — identical UUID semantics all OS.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.4.0

dependencies:
  proximity_protocol:
    path: ../protocol
  meta: ^1.12.0
  dbus: ^0.7.15

dev_dependencies:
  test: ^1.25.0
  lints: ^5.0.0

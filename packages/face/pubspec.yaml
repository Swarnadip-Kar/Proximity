name: proximity_face
description: Face detection + embedding + liveness + SK gate (EdgeFace-XS).
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.4.0

dependencies:
  proximity_protocol:
    path: ../protocol

dev_dependencies:
  test: ^1.25.0
  lints: ^5.0.0

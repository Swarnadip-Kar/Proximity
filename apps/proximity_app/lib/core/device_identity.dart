// App-install identity for the single-student-device binding.
//
// M6 sync refactor: implementation folded into core/sync/claim.dart. This
// file is the compatibility re-export — same function names
// (getOrCreateInstallId/newInstallId), so callers don't change.
library;

export 'sync/claim.dart';

# proximity_app
Single-app Student + Professor modes. See [root README](../../README.md).
Run: `flutter analyze && flutter test`; sim previews via `PROX_MODE=student|prof|enroll|take` (debug builds only — release ignores the flag).
